# Strategy used

The main tools including SRE module and mainly transform block utils are tests

Tests directory: `./src/tests`

Run the tests from the project enviornment
`python -m pytest src/tests -v`