# ── Athena / OMOP vocabulary source ────────────────────────────────────────
#
# DB_FRESH=TRUE   → run live SQL queries against your OMOP CDM instance.
#                   Requires DB_USER / DB_PASSWORD / DB_HOST / DB_NAME.
#
# DB_FRESH=FALSE  → (default) fetch the pre-built snapshot bundle from:
#                   https://github.com/strahinjastevanovic/ETL_HemOnc_regimens/raw/athena_mirrors/OTHER_REF/AthenaFeb2025snapshot.pkl
#                   No database credentials required.
#
# ────────────────────────────────────────────────────────────────────────────
DB_FRESH=FALSE

# Required only when DB_FRESH=TRUE:
DB_USER=<USERNAME>
DB_PASSWORD=<PASSWORD>
DB_HOST=<HOST>
DB_NAME=<DB>